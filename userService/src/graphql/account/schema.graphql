scalar Date

schema {
  query: Query
}

type Query {
  me: Account!
  account(id: ID!): Account
}


interface Node {
  id: ID!
}

union SearchResult = Account

type Account implements Node {
  id: ID!
  username: String!
  email: String!
}
